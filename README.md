# GLPI 资产出库单插件

## 功能
- 资产查询（计算机、显示器、打印机、网络设备）
- 勾选加入出库单
- 自动生成唯一签收编号
- 打印出库单（带公司Logo、签名栏）
- 保存历史记录到数据库
- 支持多语言（中文/英文，后续可扩展）

## 安装
1. 将 `asset_checkout` 文件夹放到 GLPI `plugins/` 目录
2. 登录 GLPI → 管理 → 插件 → 安装 → 启用插件
3. 在菜单中点击 **资产出库单** 使用

## 数据表
- `glpi_assetcheckout` 保存出库单信息
